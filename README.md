# Phase 1 Week 3 – Functions & Type Hints

## Topics Covered

### 2. Parameters and Return Values
- Demonstrates the difference between `print` and `return` in functions.
- Covers **positional**, **keyword**, and **default** parameters with real-world examples (`pizza_order`, `book_ticket`).
- Shows how default parameters act as fallback values when arguments are omitted.
